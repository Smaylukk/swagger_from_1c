
&НаСервере
Процедура ПередЗаписьюНаСервере(Отказ, ТекущийОбъект, ПараметрыЗаписи)
	Если Не ПустаяСтрока(Объект.value) Тогда
		Попытка
			value = ОбщегоНазначенияSwagger.СформироватьОбъектаНаОснованииJSON(Объект.value);
		Исключение
			Отказ = Истина;
			ОбщегоНазначения.СообщитьПользователю("В поле ""value"" должен быть валидный json");
		КонецПопытки;
	КонецЕсли;
КонецПроцедуры
