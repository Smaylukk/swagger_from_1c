
&НаСервере
Процедура ПередЗаписьюНаСервере(Отказ, ТекущийОбъект, ПараметрыЗаписи)
	Если Не ПустаяСтрока(Объект.example) Тогда
		Попытка
			example = ОбщегоНазначенияSwagger.СформироватьОбъектаНаОснованииJSON(Объект.example);
		Исключение
			Отказ = Истина;
			ОбщегоНазначения.СообщитьПользователю("В поле ""example"" должен быть валидный json");
		КонецПопытки;
	КонецЕсли;
КонецПроцедуры
