
&НаСервере
Процедура ПередЗаписьюНаСервере(Отказ, ТекущийОбъект, ПараметрыЗаписи)
	Если Не ПустаяСтрока(Объект.json) Тогда
		Попытка
			Значение = ОбщегоНазначенияSwagger.СформироватьОбъектаНаОснованииJSON(Объект.json);
		Исключение
			Отказ = Истина;
			ОбщегоНазначения.СообщитьПользователю("В поле ""json"" должен быть валидный json");
		КонецПопытки;
	КонецЕсли;
КонецПроцедуры
